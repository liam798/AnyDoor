# 验证与发布

## 工具链与版本

当前配置为 Gradle Wrapper 8.12、AGP 8.7.0、Kotlin 1.9.0，compileSdk 35、minSdk 23，
示例 targetSdk 35，Java/Kotlin 字节码目标 17。这些是仓库配置值，不代表最新版本。
SDK、插件和依赖版本以 `gradle/libs.versions.toml` 为准；Wrapper 版本在其 properties 文件。
准备兼容 AGP 的 JDK（17 或更高兼容版本）和 Android SDK，在未提交的 local.properties 配置 sdk.dir。

## 本地验证

```bash
./gradlew :anydoor:assembleRelease :sample:assembleDebug :sample:assembleDebugAndroidTest
./gradlew :anydoor:testDebugUnitTest :anydoor:lintRelease
./gradlew :anydoor:generatePomFileForReleasePublication
```

| 文件/目录 | 用途 |
| --- | --- |
| anydoor/build/outputs/aar/anydoor-release.aar | SDK 产物 |
| anydoor/build/libs/anydoor-sources.jar | 去重后的源码包 |
| sample/build/outputs/apk/debug/sample-debug.apk | 默认示例 |
| sample/build/outputs/apk/androidTest/debug/sample-debug-androidTest.apk | 仪器化测试 APK |
| anydoor/build/test-results/testDebugUnitTest/ | 单元测试 XML 证据 |
| anydoor/build/reports/tests/testDebugUnitTest/ | 单元测试报告 |
| anydoor/build/reports/lint-results-release.html | SDK Lint 报告 |
| anydoor/build/publications/release/pom-default.xml | Maven 元数据 |

测试职责：

- AnyDoorTest：门面适配、初始化、可选回调、本地绑定、显式重新注册及注销期间死亡清理。
- AdsSourceApiTest：直接编译文档源码，验证参数原值、布尔结果、失败行为与注册/注销。
- ConnectionTest：冷连接并发、连接死亡与重新发现、Provider 发现死亡重试上限及其他异常不重试。
- RegistryTest：延迟死亡通知、注册失败重试、并发注册注销、快照复用、失效和只读保护。
- AnyDoorServiceTest：入参校验、代理往返、责任链、有界队列、异常回调及死亡异常后停止分发。
- PayloadTest：API 23/33 载荷、自定义类型、Map 深度边界和循环引用。
- ProtocolTest：API 23/33 结果编码、业务 Map、旧载荷回退及不支持事务。
- ProviderTest：服务发现、服务身份与调用。

Robolectric 的本地代理往返不等同于真实系统跨进程 Binder。

## 设备验证

先检查 `adb devices -l` 和有超时保护的系统属性查询，确认设备真正响应，不自动重启共享模拟器。
在可用设备上分别执行：

```bash
./gradlew :sample:connectedDebugAndroidTest
./gradlew :sample:connectedDebugAndroidTest -PanydoorProcess=:anydoor
```

CrossProcessTest 验证主进程注册、远端同步/异步调用及注销。
RemoteEndpointTest 验证子进程注册、嵌套 Map、回调、注销、处理器进程重启后重新注册，
并包含 100 次预热、1000 次带断言的延迟采样。该采样不是竞品基准。
CenterRecoveryTest 在独立中心模式下终止样例应用自己的中心进程，验证宿主存活、
按需重连、没有自动恢复注册、同实例显式重新注册、同步/异步恢复及注销。
默认中心与宿主同进程时，该用例按条件跳过；不能将该跳过记录算作中心死亡验证通过。

## 验证记录

### 中心死亡恢复验证

2026-09-12 新增 CenterRecoveryTest，发现并修复 Provider 稳定依赖的死亡竞态：

- 修复前独立中心死亡后首次发现返回空，系统随后以 `depends on provider` 为由终止测试宿主。
- 发现链路改用非稳定 Provider 客户端，finally 释放；仅 DeadObjectException 时重新获取一次。
- 业务调用、注册请求及结果回调均不因此自动重放；公开 API 与 IPC 编码未改变。
- 新增 3 项 ConnectionTest，正式单测共 72 项通过，0 失败、0 错误、0 跳过。
- Android 16 模拟器独立中心模式下 5 项仪器化测试通过，包含实际终止中心进程后的恢复。
- 默认中心模式下 4 项通过、1 项按条件跳过，XML 报告为 tests=5、skipped=1。
- SDK Release 构建通过；Release Lint 0 错误、10 条警告；SDK XML 与 Gradle 弃用提示仍存在。
- 当前最后一次示例构建不带 anydoorProcess 参数，本地产物使用默认中心进程。

该用例覆盖一次中心退出后恢复，不代表全部死亡时序、在途请求故障、厂商真机、
Release/R8 后集成或长期压力验证已经完成。

### 快照分配测量

2026-09-12 针对 HandlerRegistry 的稳定注册状态进行本地 JVM 分配测量：
每种规模预热 100,000 次，再读取 100,000 次；通过 JVM 线程分配计数器读取分配量，
用 volatile 引用接收快照，避免返回值未使用而被消除。

| 同命令处理器数 | 优化前分配字节 | 优化后分配字节 |
| --- | --- | --- |
| 1 | 7,021,936 | 136 |
| 8 | 10,400,048 | 48 |

缓存将快照列表构建从每次调用移动到注册表变更后的首次读取；保留短锁，
并增加每个已读取命令的一份快照缓存。剩余字节包含测量开销。
这是单次宿主 JVM 对照，不是 Android ART 分配基准、IPC 延迟排名或总体性能提升比例。
临时测量用例不纳入 SDK 和正式测试源集。

本次优化后验证：

- 新增 5 项 RegistryTest，正式单测共 69 项全部通过，0 失败、0 错误、0 跳过。
- 临时测量曾随 69 项正式测试共同执行，合计 70 项通过；随后移除临时源集配置重新执行正式测试通过。
- SDK Release 构建通过；Release Lint 0 错误、10 条警告。
- Android 16 模拟器默认中心与 `:anydoor` 独立中心各 4 项仪器化测试重新执行通过。
- 该次最后一次示例构建采用 `-PanydoorProcess=:anydoor`；当时尚未覆盖中心进程死亡恢复和长期压力。

### 审查修复验证

2026-09-12 修复责任链死亡异常后继续分发、注销期间死亡导致本地绑定残留：

- 新增 5 项正式回归测试；修复前定向执行 20 项测试，3 项如预期失败，修复后全量 64 项通过。
- 同步和异步调用均验证：死亡异常不再触发下一处理器，死亡记录被清理，后续独立请求仍可执行。
- 注销验证覆盖死亡异常、存活状态变化、不重新发现服务，以及其他存活服务失败后显式重试。
- SDK Release、示例 Debug 和仪器化测试 APK 构建通过。
- SDK Release Lint 为 0 错误、10 条警告；仍有 SDK XML 版本差异和 Gradle 弃用提示。
- Android 16 模拟器 small_phone 上，默认中心与 `:anydoor` 独立中心各 4 项仪器化测试全部通过。
- 首次设备执行暴露 ResultReceiver 匿名测试类无法在子进程加载的问题；改为传递系统类型后重跑通过。

设备测试覆盖现有同步/异步调用、嵌套 Map、注销、处理器进程重启和热调用采样。
新增异常竞态通过本地故障注入验证，不等同于设备上精确复现该竞态。
该轮尚未完成中心进程死亡恢复、厂商真机、Release/R8 后集成及长期压力测试。
该轮最后一次示例构建与设备测试使用 `-PanydoorProcess=:anydoor`。

### 源码接入示例验证

2026-09-12 执行 `:anydoor:testDebugUnitTest :anydoor:assembleRelease :anydoor:releaseSourcesJar`：

- 59 项单元测试通过，0 失败、0 错误、0 跳过，包含新增的 7 项 AdsSourceApiTest。
- 直接编译 docs/examples/ads 中的交付示例，未维护测试用的源码副本。
- Release 构建及源码包任务通过，发布类库和源码包均不包含 ADS 示例及测试类。
- 文档内部链接和代码围栏检查通过。
- 仍有 SDK XML 版本差异与 Gradle 弃用提示；本次未重跑 Lint、示例 APK 或设备测试。

### 核心重构验证

此前已基于新包名、服务命名及 Map 深度保护执行验证：

- 52 项单元测试通过，0 失败、0 错误、0 跳过。XML 报告记录时间为 2026-09-11 16:16 UTC。
- SDK Release、示例 Debug、测试 APK 构建通过；默认中心和 `:anydoor` 配置均构建验证。
- 专用中心合并 Manifest 确认为 com.anydoor.AnyDoorProvider，进程为 `:anydoor`。
- Release Lint 无错误，仍有 10 条警告；构建还提示 SDK XML 工具版本差异与 Gradle 弃用项。
- POM 坐标为 com.anydoor:anydoor:0.1.0；实际 AAR 类、示例 DEX 及源码路径未发现旧厂商前缀。
- 源码 JAR 有 12 个唯一源码文件，无重复条目；字节码确认 Java 双参数 callAsync 重载存在。
- 文档内部链接、代码围栏及构建 XML 已检查。
- 设备预检属性查询仍超过 10 秒，本轮未运行仪器化业务断言；此前 connected 测试曾报告设备不兼容。

该次历史验证的最后一次示例构建采用 `-PanydoorProcess=:anydoor`；
不带该参数重新构建即可恢复示例默认配置。该次记录不代表设备验收通过，
最新设备验证结果以本节的“中心死亡恢复验证”为准。

共享 Gradle 分发与缓存曾缺失，本轮可使用临时目录的同版本 Gradle 8.12，
并传 `-Pkotlin.incremental=false` 验证；这是环境恢复手段，不是项目新增构建依赖。
故障细节保留在 `.learnings/ERRORS.md`。

## 本地发布与迁移

```bash
./gradlew :anydoor:publishReleasePublicationToMavenLocal
```

发布坐标 `com.anydoor:anydoor:0.1.0`，包含 Release AAR 和源码包。
当前只配置本地 Maven 发布，生成 POM 或构建通过不代表完成公共仓库发布。
裸 AAR 不自动解析依赖；优先使用 Maven 元数据。
包名、服务描述符和回调签名迁移要求见 [API 与接入](API与接入.md)。

迁移后核对 AAR 类路径、sources JAR、合并 Manifest、POM 和 consumer 混淆规则，
避免旧包名残留。对外发布前确认源码授权，不能将重命名视为清除来源责任。

## 生产门槛

亿级装机目标需要以下证据，不以“架构清晰”或“单测通过”替代：

- 覆盖宿主支持的系统、厂商、低内存设备及 R8 后集成。
- 分别验证中心与处理器进程死亡、注册恢复、长时间运行和反复启停。
- 覆盖队满、慢处理器、慢回调、大载荷和异常对象序列化。
- 按真实拓扑和载荷采样冷连接、热调用 P50/P95/P99、内存、线程、拒绝率。
- 宿主记录崩溃、ANR 和业务失败，分阶段灰度并准备回滚。

当前没有 SDK 级超时/取消、注册确认结果或结构化错误回调。
Map 深度保护不覆盖任意 Parcelable/Serializable/Bundle 的内部对象图，也不限制总字节数。
