<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".SampleApplication"
        android:label="AnyDoor 任意门"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:allowBackup="false">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".RemoteService" android:process=":remote" android:exported="false" />
        <service android:name=".RemoteEndpointService" android:process=":endpoint" android:exported="false" />
        <provider
            android:name="com.anydoor.AnyDoorProvider"
            android:authorities="${applicationId}.anydoor"
            android:process="${anydoorProcess}"
            android:exported="false" />
    </application>
</manifest>
